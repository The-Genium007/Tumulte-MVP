# =============================================================================
# Prometheus Targets - Applications Tumulte (Production)
# =============================================================================
#
# Ce fichier est regénéré par scripts/deploy.sh
#
# =============================================================================

- targets:
    - 'tumulte-backend-prod:3333'
  labels:
    service: 'backend'
    app: 'tumulte'

# Note: Frontend (Nuxt 3) does not expose a /metrics endpoint.
# Frontend health is monitored via cAdvisor container metrics instead.
